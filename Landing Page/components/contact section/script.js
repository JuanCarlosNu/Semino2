document.getElementById("contactForm").addEventListener("submit", function(event) {
    event.preventDefault();

    // You can add code here to handle form submission, such as sending an email or saving the data.
    // For now, this example simply prevents the default form submission behavior.
});
